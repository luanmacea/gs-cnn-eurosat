# Colab: só instala o que o Colab não traz ou precisa de versão específica.
# numpy, scikit-learn, matplotlib, seaborn, pandas e pillow já vêm no Colab
# e não precisam ser re-instalados (re-instalação causa conflito com JAX/cupy).
tensorflow==2.17.1
tensorflow-datasets==4.9.6
streamlit==1.38.0
